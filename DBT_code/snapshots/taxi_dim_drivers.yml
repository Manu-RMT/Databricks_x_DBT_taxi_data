snapshots:
  - name: taxi_dim_drivers
    relation: ref("ephemeral_drivers")
    config:
        schema: 03_gold
        database: workspace
        unique_key: driver_id
        strategy: timestamp
        updated_at : modified_at
        dbt_valid_to_current: "to_date('9999-12-31')"
