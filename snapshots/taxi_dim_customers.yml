snapshots:
  - name: taxi_dim_customers
    relation: source("silver_layer","taxi_silver_customers")
    config:
        schema: 03_gold
        database: workspace
        unique_key: customer_id
        strategy: timestamp
        updated_at : modified_at
        dbt_valid_to_current: "to_date('9999-12-31')"
